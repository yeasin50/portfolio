targets:
  $default:
    sources:
      - "lib/**"
      - "test/**"
      - "pubspec.yaml"
    builders:
      json_serializable:
        options:
          field_rename: snake
          explicit_to_json: true
      freezed:freezed:
        enabled: true
        generate_for:
          - lib/**
