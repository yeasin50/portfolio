name: about
description: A new Flutter package project.
version: 0.0.1
homepage: none

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blurhash: ^0.7.0
  intl: ^0.18.1
  my_utils:
    git:
      url: https://github.com/yeasin50/my_utils.git
      ref: master
  url_launcher: ^6.1.14

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
 